# Build output
/dist/
/zig/zig-out/
/zig/.zig-cache/
/go/zuid-go
/go/*.wasm

# Vendored build inputs (fetched/refreshed by cicd, not committed)
/zig/vendor/

# Run logs, profiles, and demo renders. Rotated by cicd; assets/demo.gif is the
# one copy that is committed.
/cicd/artifacts/

# Editor / OS
*.swp
.DS_Store
